import styled from "styled-components"

export const SetThemeContainer = styled.div`
	p {
		color: var(--color-1);
		font-weight: 500;
	}
	.themes {
		margin-top: 0.5rem;
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}
`

import styled from "styled-components"

export const SetSoundContainer = styled.div`
	p {
		color: var(--color-1);
		font-weight: 500;
	}
	div {
		margin-top: 0.5rem;
		display: flex;
		gap: 1rem;
		align-items: center;
		font-size: 2rem;
	}
`

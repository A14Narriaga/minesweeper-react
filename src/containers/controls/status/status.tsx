import { AtmIcon } from "@src/shared"

import { StatusContainer } from "./_status.styled"

export const Status = () => {
	return (
		<StatusContainer>
			<AtmIcon name="status-playing" />
		</StatusContainer>
	)
}
